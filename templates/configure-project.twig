{% extends "global.twig" %}

{% block content %}
    <h1>{{ project.name }}</h1>

    <div class="row">
        <div class="col-sm-6">

            <form target="/project/{{ project.id_project }}/configure" method="post">
                <div class="form-group">
                    <label for="path">racine</label>
                    <input class="form-control" id="path" name="path" type="text" value="{{ project.path }}">
                </div>
                <button type="submit" class="btn btn-default">configurer</button>
            </form>
        </div>
    </div>
{% endblock %}
